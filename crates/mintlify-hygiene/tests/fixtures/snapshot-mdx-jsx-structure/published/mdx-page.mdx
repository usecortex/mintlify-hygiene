---
title: MDX Structure
description: 'Exercises JSX, expressions, and ESM'
---

import Foo from "./foo.js"

export const value = 1

<Tabs>
<Tab title="Overview" />
</Tabs>

{value > 0 ? "ready" : "waiting"}

This view should render in <200ms for healthy tenants.

Query mode—when enabled—adds extra context.
